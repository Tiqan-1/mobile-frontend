/**
 * @deprecated Import from `@/theme/tokens/colors` instead.
 *
 * This module is kept for backward compatibility. New code should import
 * `PALETTELIGHT` / `PALETTEDARK` directly from `@/theme/tokens/colors` and
 * select the active palette via `useTheme().colors`.
 */
export { PALETTE, PALETTEDARK, PALETTELIGHT } from './tokens/colors';
